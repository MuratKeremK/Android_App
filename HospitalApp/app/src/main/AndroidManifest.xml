<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hastaneuygulamasi">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".PickPatient"></activity>
        <activity android:name=".DoctorAppointments" />
        <activity android:name=".DoctorMainActivity" />
        <activity android:name=".DoctorLogInActivity" />
        <activity android:name=".MyAppointmentsActivity" />
        <activity android:name=".AppointmentAcivity" />
        <activity android:name=".AccountActivity" />
        <activity android:name=".PickDoctor" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".FeedActivity" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity" />
        <activity android:name=".LogInScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>